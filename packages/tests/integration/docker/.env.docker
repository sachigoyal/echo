# Docker-specific environment overrides
# These values override .env.test when running in Docker containers

# Database configuration for Docker network
DATABASE_URL=postgresql://test:test@postgres-test:5432/echo_integration_test

# Service URLs for Docker network
ECHO_CONTROL_URL=http://echo-control-test:3000

# Docker environment flag
DOCKER_ENVIRONMENT=true

# All other variables inherit from .env.test